{
  "rules": {
    ".read": false,
    ".write": false,
    "launch_status": {
      ".read": true,
      ".write": true,
      ".validate": "newData.isString() && (newData.val() === 'waiting' || newData.val() === 'launched')"
    }
  }
}
